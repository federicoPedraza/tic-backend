export interface LoginResponse {
  access_token: string;
}

export interface SignupRespone {
  username: string;
  id: string;
  access_token: string;
}

export interface HealthResponse {
  email: string;
  id: string;
}
